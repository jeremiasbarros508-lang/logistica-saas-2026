from sqlalchemy import Column, Integer, String, ForeignKey\nfrom sqlalchemy.orm import relationship\n\nclass User(Base):\n    __tablename__ = 'users'\n    id = Column(Integer, primary_key=True)\n    username = Column(String, unique=True, nullable=False)\n    email = Column(String, unique=True, nullable=False)\n    password = Column(String, nullable=False)\n\nclass Entrega(Base):\n    __tablename__ = 'entregas'\n    id = Column(Integer, primary_key=True)\n    user_id = Column(Integer, ForeignKey('users.id'), nullable=False)\n    status = Column(String, nullable=False)\n    delivery_address = Column(String, nullable=False)\n\n    user = relationship('User', back_populates='entregas')\n\nclass Motorista(Base):\n    __tablename__ = 'motoristas'\n    id = Column(Integer, primary_key=True)\n    name = Column(String, nullable=False)\n    vehicle_id = Column(Integer, ForeignKey('veiculos.id'), nullable=False)\n\n    vehicle = relationship('Veiculo', back_populates='motoristas')\n\nclass Veiculo(Base):\n    __tablename__ = 'veiculos'\n    id = Column(Integer, primary_key=True)\n    plate_number = Column(String, unique=True, nullable=False)\n    model = Column(String, nullable=False)\n    motorista = relationship('Motorista', back_populates='vehicle')\n\nclass Rota(Base):\n    __tablename__ = 'rotas'\n    id = Column(Integer, primary_key=True)\n    origem = Column(String, nullable=False)\n    destino = Column(String, nullable=False)\n    entrega_id = Column(Integer, ForeignKey('entregas.id'))\n\n    entrega = relationship('Entrega', back_populates='rotas')\n\nUser.entregas = relationship('Entrega', order_by=Entrega.id, back_populates='user')\nEntrega.rotas = relationship('Rota', order_by=Rota.id, back_populates='entrega')\n